import {View, Text} from 'react-native';
import React from 'react';

const WeConnectScreen = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{color: 'black'}}>Third Party App(We Connect Screen)</Text>
    </View>
  );
};

export default WeConnectScreen;
