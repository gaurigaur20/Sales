import {View, Text} from 'react-native';
import React from 'react';

const MmiScreen = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{color: 'black'}}>Third Party(MMI screen)</Text>
    </View>
  );
};

export default MmiScreen;
